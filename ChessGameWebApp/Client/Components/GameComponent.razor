@using ChessGame
@inject HttpClient Http
@inherits GameComponentModel

<h3>GameComponent</h3>


@code {

    protected override async Task OnInitializedAsync()
    {
        try
        {
            Board = new ChessBoard();
            await webApi.Update(Board);
        }
        catch (Exception ex)
        {

        }
    }

}

<table class="ChessBoard" id="board">

    @for (int i = 0; i < 8; i++)
    {
        <tr>
            @for (int j = 0; j < 8; j++)
            {
                <CellComponent IsMarked = "false" Row="@i" Column="@j" Figure="@Board?[i, j]" FigureName="@Board.GetCell(i, j).FigureName" ParentComponent="@this"></CellComponent>
            }
        </tr>
    }
</table>
