@page "/game"
<h3>Game</h3>

@code {

}

<table class="ChessBoard">

    @for (int i = 0; i < 8; i++)
    {
        <tr>
            @for (int j = 0; j < 8; j++)
            {
                <td class="@(i % 2 == j % 2 ? "white" : "black")">
                    <img src="" alt="" />
                </td>
            }
        </tr>
    }
</table>
